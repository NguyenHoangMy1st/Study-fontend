// 1. textContent -> lấy ra nội dung (only text)
const spinner = document.querySelector("#spinner");
// thay đổi textContent
// spinner.textContent = "Hello new content";
console.log(spinner.textContent);

// 2. innerText